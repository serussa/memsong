概率偏置软对齐的设计思路

核心思想
在复杂版 PhaseMemory 的现有流程中，相位更新已经包含了：
复数旋转（由 omega决定）
熵门控（alpha控制更新强度）
自适应锚点（anchor提供长期参考）
轨迹记忆（traj提供短期正反馈）
现在你要加入的是：在所有这些之后、注入 hidden states 之前，根据 beat phase 预测一个相位偏移 delta，并从概率分布中采样得到它，然后旋转当前相位状态。这个 delta不是硬性拉到 beat phase 上，而是作为“建议偏移量”，模型可以自主决定是否采纳（通过 mu和 sigma控制）。
关键设计决策
插入位置：在“update”完成（得到 zr_new, zi_new）之后、轨迹记忆和 stabilize 之前。这样偏置作用于已经过熵门控和锚点调制的状态，后续 stabilize 可以保证幅度稳定。
偏置网络输入：需要包含三类信息：
当前相位状态（zr_new, zi_new）：让网络知道当前位置。
去噪进度（step_ratio）：让网络知道当前是前期还是后期，前期偏置可大些，后期偏置宜小。
beat phase（bp）：提供节拍参考，但网络可以学会忽略它。
输出：两个标量（或向量）——mu和 log_var，表示高斯分布的均值和方差。mu控制系统性偏移（如抢拍/落拍倾向），log_var控制不确定性（弹性程度）。
采样：训练时使用重参数化技巧（delta = mu + exp(0.5*log_var) * eps），推理时直接使用 mu（确定性）或也采样（如果需要随机性）。
幅度限制：delta必须限制在合理范围内（如 ±0.3 rad），防止单步相位跳跃过大。可以用 tanh缩放实现。
损失设计：除了 diffusion loss，增加一个 KL 散度项，鼓励 N(mu, sigma^2)接近标准正态 N(0,1)。这样模型不会滥用偏置，只在必要的时候偏离节拍。
与现有组件的交互
熵门控：两者独立。熵门控控制的是“更新强度”（alpha），偏置控制的是“偏移量”（delta）。可以同时使用。
自适应锚点：锚点提供长期参考，偏置提供局部弹性。不冲突。
轨迹记忆：轨迹记忆是正反馈，偏置是外加偏移。两者可能互相影响，建议先保留轨迹记忆，观察效果。如果出现不稳定，可以降低轨迹记忆的反馈系数或去掉它。
Stabilize：偏置后的状态仍会经过 stabilize（除以 sqrt(z^2+1)），所以幅度不会爆炸。
训练时的注意事项
如果某个样本没有 beat phase（如无节拍），应将 beat_phase设为 None，此时跳过偏置逻辑，delta=0，KL 损失也不计算。
lambda_kl建议从 0.001 开始，观察 KL 损失的量级，调整到约为 diffusion loss 的 1%~5%。
训练初期，bias_net的输出接近零（因为零初始化），所以偏置几乎为零，模型行为与原版一致。随着训练进行，偏置逐渐激活。
推理时的注意事项
默认使用 mu作为确定性偏置，保证生成结果稳定。
如果需要用户控制弹性，可以暴露一个缩放因子，乘到 delta上。
如果不提供 beat phase，模块退化为原行为。